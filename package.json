{
  "name": "jagafinance",
  "private": true,
  "version": "0.1.0",
  "description": "B2B Receipt & Expense Intelligence Platform",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:generate": "turbo --filter=@jagafinance/db generate",
    "db:migrate": "turbo --filter=@jagafinance/db migrate:dev",
    "db:push": "turbo --filter=@jagafinance/db push",
    "db:studio": "turbo --filter=@jagafinance/db studio",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:reset": "docker-compose down -v && docker-compose up -d",
    "blockchain:compile": "pnpm --filter=@jagafinance/blockchain compile",
    "blockchain:deploy": "pnpm --filter=@jagafinance/blockchain deploy:base-sepolia"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.5.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
