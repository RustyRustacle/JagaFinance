{
  "name": "@jagafinance/blockchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "verify:sepolia": "echo 'Usage: pnpm verify:sepolia <DEPLOYED_ADDRESS>' && hardhat verify --network sepolia",
    "test": "hardhat test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.5.0",
    "tsx": "^4.16.2",
    "chai": "^4.3.7",
    "@types/chai": "^4.3.5",
    "@types/node": "^20.14.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
